# Copy this file to `.env` (or put the value in .streamlit/secrets.toml) and
# fill in your real key. Only a Groq key is needed — embeddings run locally.
# Get a Groq key at https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here
